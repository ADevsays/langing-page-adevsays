---
const {className} = Astro.props;
---
<section class={`${className ?? 'flex flex-col lg:flex-row items-start justify-start'} pt-40 pb-20 w-full z-20 lg:px-[9rem] px-6 gap-0 lg:gap-12 min-h-[550px]`}>
    <slot/>
</section>