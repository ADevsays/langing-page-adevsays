<aside class={`${Astro.props.class} p-2 lg:p-0 filter advisory-img relative lg:my-0 my-6 w-full xl:max-w-[580px] object-contain`}>
    <img 
        class="h-full advisory-img w-full rounded-xl shadow-md shadow-gray-600" 
        src={`/resources/advisory/${Astro.props.src}`} alt={Astro.props.alt}>
</aside>

<style>
    .advisory-img{
        animation: fade-blur linear both;
        animation-timeline: view();
        animation-range: entry 35% cover 45%;
    }
    @keyframes fade-blur {
        from{
            filter: blur(1px);
            perspective: 1000px;
            transform: rotateX(32deg) translateZ(-35px); 
        }
        to{
            transform: rotateX(0deg) translateZ(-0px); 
            filter: blur(0px);
        }
    }
</style>